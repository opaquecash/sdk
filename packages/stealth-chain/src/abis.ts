import StealthAddressAnnouncer from "./abis/StealthAddressAnnouncer.json";
import StealthMetaAddressRegistry from "./abis/StealthMetaAddressRegistry.json";

/** Viem-compatible ABI for `StealthAddressAnnouncer` (announce + Announcement event). */
export const stealthAddressAnnouncerAbi = StealthAddressAnnouncer;

/** Viem-compatible ABI for `StealthMetaAddressRegistry`. */
export const stealthMetaAddressRegistryAbi = StealthMetaAddressRegistry;
