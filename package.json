{
  "name": "opaque-sdk",
  "private": true,
  "description": "Opaque monorepo: stealth (EIP-5564) + PSR TypeScript packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @opaquecash/stealth-core build && npm run -w @opaquecash/stealth-wasm build && npm run -w @opaquecash/psr-core build && npm run -w @opaquecash/stealth-chain build && npm run -w @opaquecash/stealth-balance build && npm run -w @opaquecash/psr-prover build && npm run -w @opaquecash/psr-chain build && npm run -w @opaquecash/opaque build",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "~5.6.3"
  }
}
